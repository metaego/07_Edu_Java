public class Hello3 {

    // 필드 정의
    int age;

    // int -> 키워드
    // age -> 식별자(변수)

    int myAge = 10;

    // 메서드(함수)

    // 두개의 숫자값을 입력받아서 높은 값을 구하는 메서드
    // 매개변수(필수는 아님)
    // 리턴타입(필수는 아님)
    int maxNumber(int x, int y){
        return 0;
    }

    // 리턴타입이 없는 경우 void를 사용
    void maxNumber(){
    }

}
